= link_to "Nub", root_path, :class => 'brand'
%ul.nav
  - if person_signed_in?
    %li
      = link_to 'Log out', destroy_person_session_path, :method=>'delete'
    - if current_person.has_role? :admin
      %li
        = link_to 'Admin', people_path
  - else
    %li
      = link_to 'Sign in with Twitter', new_person_session_path
